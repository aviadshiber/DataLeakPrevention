package org.example.rules.conditions;
// this package was created, so we can have static binding to the package, make refactoring easier
public class PackageInfo {
    // all classes in this package should be stateless
}
